name: Notifications µS

on:
  push:
    branches:
      - main
    paths:
      - "services/notifications/**"

jobs:
  deploy:
    uses: ./.github/workflows/deploy-microservice.yml
    with:
      service: notifications