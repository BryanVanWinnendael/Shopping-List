name: Category Model µS

on:
  push:
    branches:
      - main
    paths:
      - "services/category-model/**"

jobs:
  deploy:
    uses: ./.github/workflows/deploy-microservice.yml
    with:
      service: category-model