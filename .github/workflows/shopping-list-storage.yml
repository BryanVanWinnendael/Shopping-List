name: Storage µS

on:
  push:
    branches:
      - main
    paths:
      - "services/storage/**"

jobs:
  deploy:
    uses: ./.github/workflows/deploy-microservice.yml
    with:
      service: storage