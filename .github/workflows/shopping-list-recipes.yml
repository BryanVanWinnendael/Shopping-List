name: Recipes µS

on:
  push:
    branches:
      - main
    paths:
      - "services/recipes/**"

jobs:
  deploy:
    uses: ./.github/workflows/deploy-microservice.yml
    with:
      service: recipes