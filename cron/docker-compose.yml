services:
  shopping-list:
    container_name: shopping-list-cron
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - "3000"
    volumes:
      - ./serviceAccountKey.json:/serviceAccountKey.json:ro
      - ./data:/app/data
    environment:
      ENV: production
      GOOGLE_APPLICATION_CREDENTIALS: /serviceAccountKey.json
      DATA_DIR: /app/data
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - shopping-list-network
networks:
  shopping-list-network:
    external: true
