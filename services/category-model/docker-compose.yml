services:
  shopping-list:
    container_name: shopping-list-category-model
    image: ghcr.io/bryanvanwinnendael/shopping-list-category-model:${IMAGE_TAG:-latest}
    expose:
      - "3000"
    volumes:
      - ./data:/app/data
    environment:
      ENV: production
      DATA_DIR: /app/data
      CATEGORIES_FILE: categories.csv
      MODEL_FILE: model.pkl
      PORT: 3000
      LOGS_API_URL: http://shopping-list-logs:3000/api/logs
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - shopping-list-network
networks:
  shopping-list-network:
    external: true